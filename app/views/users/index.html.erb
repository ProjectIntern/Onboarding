<!DOCTYPE html>
<html>
  <head>
    <title>Sign Up</title>
  </head>

  <body>
  <div class="interns_wrapper">
    <div class="interns_container">
      <div class="interns">
         <% @users.each do |t| %>
         <%= link_to image_tag(t.image, size: "160x150"), user_path(t), method: "get", action: "show" %>
         <% end %>
       </div> 
     </div>
   </div>
  </body>
</html>