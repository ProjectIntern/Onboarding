<div class="messages_text">
	<%# <%= form_for @message do |f| %>
	<div class="log">
	<%= form_for Message.new do |f| %>
		<%#<textarea class="text_area"><%= f.text_area :description, :placeholder => "Type Comment Here" %>
		<%= f.text_area :description, :placeholder => "Type Comment Here" %>
    	<%#<%= f.text_field :user_id, :value => "#{@current_user.id}", class: "hidden" %>
    	<%= f.text_field :hire_type_str, :value => hire_type_obj.name , class: "hidden" %>
		<%= f.submit "Comment", class: "msg-submit" %><br>
	<% end %>
	</div>
 
<div id="log"></div>
 
<script>
$( "body" ).click(function( event ) {
	var target = $(event.target);
	var ta = $("textarea.text_area")
	var ta2 = $("messages_text.text_area")
	if (ta.is(target)) {
		
  $( "#log" ).html( "clicked: " + event.target.nodeName );

}
});
</script>
 

</html>

</div>